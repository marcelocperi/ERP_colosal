CREATE TABLE `imp_despachos` (
  `id` int(11) NOT NULL AUTO_INCREMENT,
  `enterprise_id` int(11) NOT NULL,
  `orden_compra_id` int(11) NOT NULL,
  `numero_despacho` varchar(40) DEFAULT NULL,
  `despachante_id` int(11) DEFAULT NULL,
  `fecha_oficializacion` date DEFAULT NULL,
  `fecha_liberacion` date DEFAULT NULL,
  `canal` enum('VERDE','AMARILLO','ROJO') DEFAULT 'VERDE',
  `transportista` varchar(100) DEFAULT NULL,
  `guia_bl_tracking` varchar(100) DEFAULT NULL,
  `vessel_mmsi` varchar(30) DEFAULT NULL,
  `vessel_name` varchar(100) DEFAULT NULL,
  `fecha_embarque` date DEFAULT NULL,
  `fecha_arribo_estimada` date DEFAULT NULL,
  `fecha_arribo_real` date DEFAULT NULL,
  `puerto_embarque` varchar(100) DEFAULT NULL,
  `puerto_destino` varchar(100) DEFAULT NULL,
  `bultos` int(11) DEFAULT NULL,
  `peso_kg` decimal(12,2) DEFAULT NULL,
  `dias_libres_puerto` int(11) DEFAULT 0,
  `costo_demora_diario_usd` decimal(12,2) DEFAULT 0.00,
  `fecha_devolucion_contenedor` date DEFAULT NULL,
  `alerta_demora_enviada` tinyint(4) DEFAULT 0,
  `estado` enum('PENDIENTE','PRESENTADO','EN_REVISION','OBSERVADO','LIBERADO','INGRESADO') DEFAULT 'PENDIENTE',
  `valor_fob_usd` decimal(18,2) DEFAULT 0.00,
  `valor_cif_usd` decimal(18,2) DEFAULT 0.00,
  `derechos_ars` decimal(18,2) DEFAULT 0.00,
  `tasa_estadistica_ars` decimal(18,2) DEFAULT 0.00,
  `otros_tributos_ars` decimal(18,2) DEFAULT 0.00,
  `tipo_cambio_oficializacion` decimal(18,6) DEFAULT NULL,
  `observaciones` text DEFAULT NULL,
  `user_id` int(11) DEFAULT NULL,
  `created_at` timestamp NULL DEFAULT current_timestamp(),
  `updated_at` timestamp NULL DEFAULT current_timestamp() ON UPDATE current_timestamp(),
  PRIMARY KEY (`id`),
  KEY `idx_despachos_orden` (`orden_compra_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci